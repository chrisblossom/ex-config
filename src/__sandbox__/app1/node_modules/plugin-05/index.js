'use strict';

function plugin05(args) {
    if (typeof args.api.add === 'function') {
        args.api.add('plugin-05');
    }

    return {
        imported: 'plugin-05',
        plugin05Args: args,
    };
}

module.exports = plugin05;
