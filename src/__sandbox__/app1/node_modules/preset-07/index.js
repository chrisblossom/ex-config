'use strict';

function preset07(args) {
    if (typeof args.api.add === 'function') {
        args.api.add('preset-07');
    }

    return {
        preset07Args: args,
    };
}

module.exports = preset07;
